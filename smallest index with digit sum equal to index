class Solution {
public:
    int smallestIndex(vector<int>& nums) {
        int n = nums.size();
        for(int i=0 ; i<n ; i++){
            int sum = 0 ;
            int n = v[i];
            while(nums[i]>0){
                sum = sum + n%10;
                n = n/10;

            }
            if(sum == i){
                return i;
            }
        }
        return -1;    
    }
};
